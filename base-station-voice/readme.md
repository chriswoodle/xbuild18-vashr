# Setup
```
npm install
```
Create `keys.json` from `keys.master.json` and add api keys from IBM watson.

# Running
```
node index
```
# Debugging
Normal debug:
```
export DEBUG=xbuild:*
node index
```

Verbose debug:
```
export DEBUG=xbuild*
node index
```